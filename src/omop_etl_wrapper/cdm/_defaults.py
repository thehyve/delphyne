"""Module containing the default schema names as used in the ORM."""

# These are the default schema names as used in the ORM table
# definitions and as keys in the schema_translate_map.
# Actual schema names can be different at runtime, as determined by the
# provided parameters when instantiating the Wrapper class
VOCAB_SCHEMA = 'vocab'
CDM_SCHEMA = 'cdm'
